# Compiled binaries
/bin/
tbc
tbc.exe

# Local runtime state: the chain database, private keys and local secrets.
# Never commit a keystore, even a toy one.
/data/
*keys.json
*.env
!.env.example

# Go test / coverage artefacts
*.out
*.test
